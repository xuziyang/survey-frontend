# 🎯 极简问卷系统 (Simple Survey App)

一个基于 React + TypeScript + Ant Design 开发的现代化问卷调查管理系统前端项目。

## ✨ 功能特性

### 📋 问卷管理
- ✅ 创建、编辑、删除问卷
- ⭐ 问卷标星/取消标星
- 📊 查看问卷统计数据
- 📝 问卷发布状态管理
- 🗑️ 回收站功能
- 📄 问卷复制功能

### 👤 用户功能
- 🔐 用户登录/注册
- 👥 用户信息展示

### 🎨 界面特点
- 响应式设计
- 直观的侧边栏导航
- 卡片式问卷列表展示
- 快捷操作按钮

## 🛠️ 技术栈

- **前端框架**: React 19.1.0
- **开发语言**: TypeScript 5.8.3
- **构建工具**: Vite 7.1.2
- **UI 组件库**: Ant Design 5.27.0
- **路由管理**: React Router DOM 7.8.0
- **样式方案**: Sass/SCSS
- **代码规范**: ESLint + Prettier

## 📦 项目结构

```
survey-frontend/
├── public/                 # 静态资源
├── src/
│   ├── assets/            # 图片等资源文件
│   ├── compoents/         # 公共组件
│   │   ├── Logo.tsx       # Logo 组件
│   │   ├── UserInfo.tsx   # 用户信息组件
│   │   ├── QuestionCard.tsx   # 问卷卡片组件
│   │   └── ListSearch.tsx     # 搜索组件
│   ├── layouts/           # 布局组件
│   │   ├── MainLayout.tsx     # 主布局
│   │   └── ManageLayout.tsx   # 管理页布局
│   ├── pages/             # 页面组件
│   │   ├── Home.tsx           # 首页
│   │   ├── Login.tsx          # 登录页
│   │   ├── Register.tsx       # 注册页
│   │   ├── NotFound.tsx       # 404 页面
│   │   ├── manage/            # 问卷管理页面
│   │   │   ├── List.tsx       # 我的问卷
│   │   │   ├── Star.tsx       # 星标问卷
│   │   │   └── Trash.tsx      # 回收站
│   │   └── question/          # 问卷操作页面
│   │       ├── Edit/          # 编辑问卷
│   │       └── Stat/          # 数据统计
│   ├── router/            # 路由配置
│   ├── App.tsx            # 根组件
│   └── main.tsx           # 入口文件
├── package.json           # 项目配置
├── vite.config.ts         # Vite 配置
└── tsconfig.json          # TypeScript 配置
```

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm >= 8.0.0 或 Yarn >= 1.22.0

### 安装依赖

使用 Yarn（推荐）:

```bash
yarn
```

或使用 npm:

```bash
npm install
```

### 启动开发服务器

```bash
yarn dev
# 或
npm run dev
```

启动后访问: http://localhost:5173

### 构建生产版本

```bash
yarn build
# 或
npm run build
```

### 预览生产构建

```bash
yarn preview
# 或
npm run preview
```

### 代码检查

```bash
yarn lint
# 或
npm run lint
```

## 🗺️ 路由结构

```
/                          # 首页
├── /login                 # 登录页
├── /register              # 注册页
└── /manage                # 管理页面
    ├── /list              # 我的问卷
    ├── /star              # 星标问卷
    └── /trash             # 回收站

/question/edit/:id         # 编辑问卷
/question/stat/:id         # 问卷统计
```

## 📝 开发规范

### 代码风格

项目使用 ESLint 和 Prettier 统一代码风格:

- 使用 TypeScript 严格模式
- 遵循 React Hooks 规则
- 组件使用函数式组件 + TypeScript
- 样式使用 SCSS Modules

### 组件规范

```typescript
import { type FC } from 'react';

type PropsType = {
  title: string;
  // ... 其他属性
};

const ComponentName: FC<PropsType> = (props) => {
  // 组件逻辑
  return (
    // JSX
  );
};

export default ComponentName;
```

## 🔧 配置说明

### Vite 配置

Vite 配置文件位于 `vite.config.ts`，可根据需要调整:

- 开发服务器端口
- 代理配置
- 构建优化选项
- 插件配置

### TypeScript 配置

项目使用严格的 TypeScript 配置，确保类型安全。

## 📚 相关资源

- [React 官方文档](https://react.dev/)
- [Ant Design 组件库](https://ant.design/)
- [Vite 构建工具](https://vitejs.dev/)
- [React Router 路由](https://reactrouter.com/)

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 开源协议

本项目采用 MIT 协议 - 详见 LICENSE 文件

## 👥 作者

- xuziyang

## 🙏 致谢

感谢所有为这个项目做出贡献的开发者！

---

⭐ 如果这个项目对你有帮助，请给它一个 Star！

